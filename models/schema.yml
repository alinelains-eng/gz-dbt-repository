version: 2

sources:
   - name: gz_raw_data
     database: project-dbt-aline
     schema: gz_raw_data
     loader: bigquery
     description: ...
     tables:
       - name: sales
         identifier: raw_gz_sales
       - name: product
         identifier: raw_gz_product
       - name: ship
         identifier: raw_gz_ship 